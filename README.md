# FindEat

Prototype autonome en un seul fichier HTML pour rechercher des primeurs et producteurs locaux partout en France.

## Utilisation
Ouvrez `index.html` directement dans un navigateur, ou activez GitHub Pages sur la branche `main`.

Le prototype utilise Leaflet et l'API Adresse de l'État via CDN/API publics.
